# 🚀 Docker + Jenkins CI/CD Pipeline (Flask App)

This project demonstrates a simple **CI/CD pipeline** using **Docker** and **Jenkins** for a containerized **Flask web application**.

---

## 🧠 What It Does
- Builds a Docker image for a Flask app  
- Runs unit tests inside the container  
- Pushes the image automatically to Docker Hub  

---

## 🧱 Tech Stack
- Python (Flask)
- Docker
- Jenkins (Pipeline + Docker Plugin)
- Pytest

---

## ⚙️ Setup Instructions

### 1. Clone Repo
```bash
git clone https://github.com/<your-username>/docker-jenkins-ci-cd.git
cd docker-jenkins-ci-cd

